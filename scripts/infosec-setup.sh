#!/bin/bash
# For connecting to VPNs
sudo apt install openvpn
# For scanning
sudo apt install nmap
# For fixing nc connections
sudo apt install rlwrap
# For proper color
sudo apt install grc
# SQLMap
sudo apt install sqlmap
# pip3 for Python3
sudo apt install python3-pip
